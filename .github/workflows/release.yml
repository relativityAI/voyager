# not useful right now, since we want to update the version in __version__.py before building and releasing, but we can revisit this later if we want to automate releases more
# name: Release and Publish

# on:
#   push:
#     tags:
#       - "v*.*.*"

# permissions:
#   contents: write

# jobs:
#   release:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#         with:
#           fetch-depth: 0

#       - name: Extract version from tag
#         id: version
#         run: |
#           TAG="${{ github.ref }}"
#           VERSION=${TAG#refs/tags/v}
#           echo "version=$VERSION" >> $GITHUB_OUTPUT
#           echo "Extracted version: $VERSION"

#       - name: Update __version__.py
#         run: |
#           VERSION="${{ steps.version.outputs.version }}"
#           cat > __version__.py << 'EOF'
#           """Version information for Voyager."""

#           __version__ = "$VERSION"
#           __description__ = "A financial data scraping and processing tool"
#           __url__ = "https://github.com/relativityAI/voyager"

#           VERSION = __version__
#           EOF
#           echo "Updated __version__.py to version $VERSION"

#       - name: Configure Git
#         run: |
#           git config user.name "github-actions[bot]"
#           git config user.email "github-actions[bot]@users.noreply.github.com"

#       - name: Commit and push version update
#         run: |
#           VERSION="${{ steps.version.outputs.version }}"
#           git add __version__.py
#           git commit -m "chore: update version to $VERSION" || echo "No changes to commit"
#           git push origin HEAD:main || echo "Already up to date"

#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: "3.10"

#       - name: Install dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install build twine

#       - name: Build package
#         run: python -m build

#       - name: Create Release
#         uses: softprops/action-gh-release@v1
#         with:
#           files: dist/*
#           draft: false
#           prerelease: false
#           generate_release_notes: true
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

#       - name: Publish to PyPI
#         run: |
#           twine upload dist/* -u __token__ -p ${{ secrets.PYPI_API_TOKEN }}
#         if: ${{ secrets.PYPI_API_TOKEN != '' }}
#         continue-on-error: true
